import React from 'react'
import styles from './Timer.module.css'

const Timer = () => {
  return (
    <div className={styles.timerCompDiv}>
      <p className={styles.timerCompP}>30</p> 
    </div>
  )
}

export default Timer